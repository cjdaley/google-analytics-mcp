[build]
dockerfile = "Dockerfile"

[deploy]
startCommand = "python -m ads_mcp.server"
healthcheckPath = "/health"
healthcheckTimeout = 100

[env]
PORT = "8000"
FASTMCP_HOST = "0.0.0.0"
FASTMCP_PORT = "8000"
