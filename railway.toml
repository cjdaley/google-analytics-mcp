[build]
builder = "RAILPACK"
buildCommand = "pip install -e . && pip install pyright"

[deploy]
startCommand = "sh -c 'python -m analytics_mcp.server'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5

[env]
PYTHONUNBUFFERED = "1"
