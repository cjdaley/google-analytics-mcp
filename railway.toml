[build]
builder = "nixpacks"
buildCommand = "pip install -e . && pip install mcp"

[deploy]
startCommand = "python -m mcp.server.stdio python -m analytics_mcp.server"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5

[env]
PYTHONUNBUFFERED = "1"
