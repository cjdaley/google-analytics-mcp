[build]
builder = "RAILPACK"
buildCommand = "pip install -e . --no-cache-dir"

[deploy]
startCommand = "python -m analytics_mcp.server"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5

[env]
PYTHONUNBUFFERED = "1"
